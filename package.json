{
  "name": "fast-wrangler-plus",
  "displayName": "FastWrangler Plus",
  "publisher": "ransananes",
  "description": "Lite, instant alternative to Data Wrangler with DuckDB-Wasm",
  "version": "0.1.3",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/ransananes/fast-wrangler"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other",
    "Data Science",
    "Machine Learning"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:fastWrangler.explore"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "fastWrangler.explore",
        "title": "FastWrangler: Explore Data",
        "icon": "$(database)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "fastWrangler.explore",
          "when": "resourceExtname == .csv || resourceExtname == .parquet",
          "group": "navigation"
        }
      ],
      "debug/variables/context": [
        {
          "command": "fastWrangler.explore",
          "when": "debugType == 'python' || debugType == 'debugpy'",
          "group": "navigation@1"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "bundle": "node scripts/bundle-duckdb.mjs",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint"
  },
  "devDependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev45.0",
    "@types/node": "^18.19.130",
    "@types/vscode": "^1.80.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.0.0"
  }
}